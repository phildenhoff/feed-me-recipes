# Code style

- Avoid unnecessary, uncommon, or ambiguous acronyms. Spell out the full term
  in identifiers, comments, and documentation (e.g. `extractOpenGraphImageUrl`,
  not `extractOgImageUrl`). The raw HTML attribute names (`og:image`) are
  fine to use as-is because they are protocol literals, not identifiers.
